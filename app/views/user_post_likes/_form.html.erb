<%= form_for(@user_post_like) do |f| %>
  <% if @user_post_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_post_like.errors.count, "error") %> prohibited this user_post_like from being saved:</h2>

      <ul>
      <% @user_post_like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
